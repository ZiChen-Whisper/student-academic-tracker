# 最终关系模式文档

**项目名称**：中小学学生学业发展动态跟踪与预警系统  
**组长**：邹梓辰  
**日期**：2026年6月1日  
**E-R图来源**：`合并ER图.drawio`

---

## 目录

- [一、关系模式清单](#一关系模式清单)
- [二、E-R图转换过程说明](#二e-r图转换过程说明)
- [三、规范化检查结果](#三规范化检查结果)
- [四、建表顺序说明](#四建表顺序说明)

---

## 一、关系模式清单

### 1. Teacher（教师表）

**关系模式**：Teacher(<u>TeacherID</u>, TeacherName, TeacherGender, TeacherTitle)

**主键**：TeacherID

**外键**：无

**说明**：存储任课教师基本信息，是项目扩展实体。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| TeacherID | VARCHAR(20) | PRIMARY KEY | 教师工号，格式：T001, T002等 |
| TeacherName | VARCHAR(50) | NOT NULL | 教师姓名 |
| TeacherGender | ENUM('M','F') | | 性别 |
| TeacherTitle | VARCHAR(30) | | 教师职称（如：高级教师、一级教师等） |

**E-R图转换过程**：
- 原E-R图：TEACHER实体，独立存在
- 转换规则：实体直接转换为关系模式
- 属性全部转换为表的列

---

### 2. Subject（科目表）

**关系模式**：Subject(<u>SubjectID</u>, SubjectName, SubjectCredit, SubjectType)

**主键**：SubjectID

**外键**：无

**说明**：存储考试科目基本信息，是项目扩展实体。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| SubjectID | VARCHAR(20) | PRIMARY KEY | 科目编号，格式：SUBJ001, SUBJ002等 |
| SubjectName | VARCHAR(50) | NOT NULL | 科目名称（如：数学、语文、英语等） |
| SubjectCredit | INT | | 科目学分 |
| SubjectType | VARCHAR(20) | | 科目类型（如：必修、选修等） |

**E-R图转换过程**：
- 原E-R图：SUBJECT实体，独立存在
- 转换规则：实体直接转换为关系模式
- 与学生的M:N联系通过中间表StudentSubject实现

---

### 3. Class（班级表）

**关系模式**：Class(<u>ClassID</u>, ClassName, ClassGrade, ClassTeacherID)

**主键**：ClassID

**外键**：ClassTeacherID → Teacher.TeacherID

**说明**：存储行政班级信息，ClassTeacherID是班主任，引用教师表。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| ClassID | VARCHAR(20) | PRIMARY KEY | 班级编号，格式：CLS001, CLS002等 |
| ClassName | VARCHAR(50) | NOT NULL | 班级名称（如：高一(1)班） |
| ClassGrade | VARCHAR(10) | | 年级（如：高一、初二等） |
| ClassTeacherID | VARCHAR(20) | FOREIGN KEY | 班主任工号，引用Teacher表 |

**E-R图转换过程**：
- 原E-R图：CLASS实体，与TEACHER通过ClassHasTeacher联系
- 转换规则：实体直接转换为关系模式，外键ClassTeacherID引用Teacher表
- 与学生的N:1联系通过学生表的StudentClassID外键实现

---

### 4. Student（学生表）

**关系模式**：Student(<u>student_id</u>, StudentName, StudentGender, StudentAge, StudentClassID, StudentAddress)

**主键**：student_id

**外键**：StudentClassID → Class.ClassID

**说明**：存储学生基本信息，数据来源于UCI Student Performance数据集和Student Performance Factors数据集。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| student_id | VARCHAR(20) | PRIMARY KEY | 学号，格式：STU_M_0001/STU_P_0001/STU_F_0001 |
| StudentName | VARCHAR(50) | NOT NULL | 学生姓名 |
| StudentGender | ENUM('M','F') | | 性别 |
| StudentAge | INT | | 年龄 |
| StudentClassID | VARCHAR(20) | FOREIGN KEY | 班级ID，引用Class表 |
| StudentAddress | VARCHAR(100) | | 地址 |

**E-R图转换过程**：
- 原E-R图：STUDENT实体，与CLASS通过StudentPlaceInClass联系（N:1）
- 转换规则：根据1:N联系转换规则，外键加在N端（学生）
- 因此`StudentClassID`作为外键添加到`student`表中，引用`Class.ClassID`

---

### 5. ExamScore（考试成绩表）

**关系模式**：ExamScore(<u>score_id</u>, student_id, subject_id, score, scoreDate, ExamStage)

**主键**：score_id

**外键**：student_id → Student.student_id, subject_id → Subject.SubjectID

**说明**：存储学生各阶段考试成绩，G1/G2/G3存为3条记录，ExamStage字段区分阶段。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| score_id | INT | PRIMARY KEY, AUTO_INCREMENT | 成绩记录ID |
| student_id | VARCHAR(20) | NOT NULL, FOREIGN KEY | 学号，引用Student表 |
| subject_id | VARCHAR(20) | NOT NULL, FOREIGN KEY | 科目ID，引用Subject表 |
| score | INT | NOT NULL | 分数（0-20分制） |
| scoreDate | DATE | | 考试日期 |
| ExamStage | VARCHAR(10) | | 考试阶段（G1/G2/G3） |

**E-R图转换过程**：
- 原E-R图：学生（1端）——StudentTakeExamScore——考试成绩（N端）
- 转换规则：根据1:N联系转换规则，外键加在N端（考试成绩）
- 因此`student_id`作为外键添加到`exam_score`表中
- 同时考试成绩与科目有关联，添加`subject_id`外键引用Subject表

---

### 6. LearningBehavior（学习行为表）

**关系模式**：LearningBehavior(<u>behavior_id</u>, student_id, AttendanceRate, StudyHours, SleepHours, MotivationLevel, PreviousScores, TutoringSessions, InternetAccess, Extracurricular, PhysicalActivity)

**主键**：behavior_id

**外键**：student_id → Student.student_id

**说明**：存储学生学习行为数据，来源于Student Performance Factors数据集。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| behavior_id | INT | PRIMARY KEY, AUTO_INCREMENT | 行为记录ID |
| student_id | VARCHAR(20) | NOT NULL, FOREIGN KEY | 学号，引用Student表 |
| AttendanceRate | INT | | 出勤率（百分比） |
| StudyHours | INT | | 学习时长（小时/周） |
| SleepHours | INT | | 睡眠时长（小时/天） |
| MotivationLevel | VARCHAR(10) | | 学习动机水平（high/medium/low） |
| PreviousScores | INT | | 之前成绩 |
| TutoringSessions | INT | | 辅导次数 |
| InternetAccess | VARCHAR(5) | | 网络访问情况（yes/no） |
| Extracurricular | VARCHAR(5) | | 课外活动参与情况（yes/no） |
| PhysicalActivity | INT | | 体育活动时长（小时/周） |

**E-R图转换过程**：
- 原E-R图：学生（1端）——StudentTakeLearningBehavior——学习行为（N端）
- 转换规则：根据1:N联系转换规则，外键加在N端（学习行为）
- 因此`student_id`作为外键添加到`learning_behavior`表中

---

### 7. FamilyBackground（家庭背景表）

**关系模式**：FamilyBackground(<u>FamilyID</u>, student_id, FatherEdu, MotherEdu, FatherJob, MotherJob, FamilyIncome, FamilySupport, ParentalInvolvement, FamRel)

**主键**：FamilyID

**外键**：student_id → Student.student_id（UNIQUE约束）

**说明**：存储学生家庭教育和经济情况，来源于UCI数据集和Performance Factors数据集。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| FamilyID | INT | PRIMARY KEY, AUTO_INCREMENT | 家庭记录ID |
| student_id | VARCHAR(20) | NOT NULL, UNIQUE, FOREIGN KEY | 学号，引用Student表，UNIQUE保证1:1 |
| FatherEdu | VARCHAR(20) | | 父亲教育水平（如：none/primary/secondary/tertiary） |
| MotherEdu | VARCHAR(20) | | 母亲教育水平 |
| FatherJob | VARCHAR(30) | | 父亲职业 |
| MotherJob | VARCHAR(30) | | 母亲职业 |
| FamilyIncome | VARCHAR(20) | | 家庭收入水平 |
| FamilySupport | VARCHAR(10) | | 家庭支持程度 |
| ParentalInvolvement | VARCHAR(10) | | 父母参与度 |
| FamRel | INT | | 家庭关系质量（1-5分） |

**E-R图转换过程**：
- 原E-R图：学生（1端）——StudentTakeFamilyBackground——家庭背景（1端）
- 转换规则：根据1:1联系转换规则，与家庭背景端合并
- `student_id`作为外键加到`family_background`表
- 加`UNIQUE`约束确保一个学生只有一条家庭记录（1:1关系）

---

### 8. RiskAlert（风险预警表）

**关系模式**：RiskAlert(<u>AlertID</u>, student_id, RiskLevel, AlertTime, RiskFactors, InterventionStatus, InterventionMeasure, InterventionResult)

**主键**：AlertID

**外键**：student_id → Student.student_id

**说明**：存储AI生成的风险预警记录，是AI功能的核心数据表。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| AlertID | INT | PRIMARY KEY, AUTO_INCREMENT | 预警记录ID |
| student_id | VARCHAR(20) | NOT NULL, FOREIGN KEY | 学号，引用Student表 |
| RiskLevel | ENUM('low','medium','high') | NOT NULL | 风险等级 |
| AlertTime | DATETIME | NOT NULL | 预警生成时间 |
| RiskFactors | TEXT | | 风险因素（JSON格式字符串） |
| InterventionStatus | ENUM('pending','in_progress','completed') | DEFAULT 'pending' | 干预状态 |
| InterventionMeasure | TEXT | | 干预措施 |
| InterventionResult | TEXT | | 干预结果 |

**E-R图转换过程**：
- 原E-R图：学生（1端）——StudentReceiveAlert——风险预警（N端）
- 转换规则：根据1:N联系转换规则，外键加在N端（风险预警）
- 因此`student_id`作为外键添加到`risk_alert`表中

---

### 9. LearningSuggestion（学习建议表）

**关系模式**：LearningSuggestion(<u>SuggestionID</u>, student_id, SuggestionContent, GenerateTime, StudentFeedback, SuggestRelateScore)

**主键**：SuggestionID

**外键**：student_id → Student.student_id

**说明**：存储AI生成的学习建议，是AI功能的核心数据表。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| SuggestionID | INT | PRIMARY KEY, AUTO_INCREMENT | 建议记录ID |
| student_id | VARCHAR(20) | NOT NULL, FOREIGN KEY | 学号，引用Student表 |
| SuggestionContent | TEXT | NOT NULL | 建议内容 |
| GenerateTime | DATETIME | NOT NULL | 建议生成时间 |
| StudentFeedback | ENUM('satisfied','neutral','unsatisfied') | | 学生反馈 |
| SuggestRelateScore | INT | | 建议相关成绩 |

**E-R图转换过程**：
- 原E-R图：学生（1端）——StudentReceiveSuggestion——学习建议（N端）
- 转换规则：根据1:N联系转换规则，外键加在N端（学习建议）
- 因此`student_id`作为外键添加到`learning_suggestion`表中

---

### 10. CourseSchedule（课程安排表）

**关系模式**：CourseSchedule(<u>ScheduleID</u>, ScheduledPeriod, ScheduledClassroom, SubjectID, TeacherID, ClassID)

**主键**：ScheduleID

**外键**：SubjectID → Subject.SubjectID, TeacherID → Teacher.TeacherID, ClassID → Class.ClassID

**说明**：存储教师授课安排，是教学与课程模块的核心数据表。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| ScheduleID | INT | PRIMARY KEY, AUTO_INCREMENT | 课程安排ID |
| ScheduledPeriod | VARCHAR(50) | | 上课时间段（如：周一第1-2节） |
| ScheduledClassroom | VARCHAR(50) | | 上课教室 |
| SubjectID | VARCHAR(20) | NOT NULL, FOREIGN KEY | 科目ID，引用Subject表 |
| TeacherID | VARCHAR(20) | NOT NULL, FOREIGN KEY | 教师工号，引用Teacher表 |
| ClassID | VARCHAR(20) | NOT NULL, FOREIGN KEY | 班级ID，引用Class表 |

**E-R图转换过程**：
- 原E-R图：课程安排（N端）分别与班级（1端）、教师（1端）、科目（1端）关联
  - ClassHasSchedule：班级 — 课程安排（1:N）
  - TeacherTeachesSchedule：教师 — 课程安排（1:N）
  - SubjectHasSchedule：科目 — 课程安排（1:N）
- 转换规则：根据1:N联系转换规则，外键都加在N端（课程安排）
- 因此`course_schedule`表包含3个外键：`SubjectID`、`TeacherID`、`ClassID`

---

### 11. StudentSubject（学生科目选修表）——M:N联系的中间表

**关系模式**：StudentSubject(<u>student_id, subject_id</u>, enroll_time)

**主键**：(student_id, subject_id) 联合主键

**外键**：student_id → Student.student_id, subject_id → Subject.SubjectID

**说明**：存储学生选修科目信息，是StudentTakeSubject（M:N联系）的中间表。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| student_id | VARCHAR(20) | NOT NULL, FOREIGN KEY | 学号，引用Student表 |
| subject_id | VARCHAR(20) | NOT NULL, FOREIGN KEY | 科目ID，引用Subject表 |
| enroll_time | DATETIME | | 选修时间 |

**E-R图转换过程**：
- 原E-R图：学生（M端）——StudentTakeSubject——科目（N端）
- 转换规则：根据M:N联系转换规则，**必须**独立成一个新表
- 主键是两个外键的组合（联合主键）
- 这是本项目**唯一的M:N联系**，需要特别关注

**为什么需要中间表**：
- M:N联系两端都是"多"，无法通过在某一端加外键来表示
- 一个学生可以选多门科目（student_id可重复）
- 一门科目可以有多个学生（subject_id可重复）
- 但同一个学生选同一门科目只能有一条记录（组合唯一）

---

### 12. NL2SQLLog（NL2SQL查询日志表）——独立系统日志表

**关系模式**：NL2SQLLog(<u>query_id</u>, user_id, natural_language_input, generated_sql, execution_time_ms, is_correct, query_time)

**主键**：query_id

**外键**：无

**说明**：存储自然语言查询记录，是系统自动记录的日志表，不在E-R图中，但需求分析需要。

**字段详细说明**：

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| query_id | INT | PRIMARY KEY, AUTO_INCREMENT | 查询记录ID |
| user_id | VARCHAR(20) | | 用户ID |
| natural_language_input | TEXT | NOT NULL | 自然语言输入 |
| generated_sql | TEXT | NOT NULL | 生成的SQL语句 |
| execution_time_ms | INT | | 执行时间（毫秒） |
| is_correct | BOOLEAN | | 是否正确 |
| query_time | DATETIME | NOT NULL | 查询时间 |

**E-R图转换过程**：
- 此表是独立系统日志表，不与任何实体关联
- 根据需求分析阶段确定的功能需求添加

---

## 二、E-R图转换过程说明

### 2.1 联系转换总结表

| 联系名 | 涉及实体 | 联系类型 | 转换方式 | 结果 |
|--------|---------|---------|---------|------|
| StudentTakeExamScore | 学生 — 考试成绩 | 1:N | 与N端合并 | `exam_score`表加`student_id`外键 |
| StudentTakeLearningBehavior | 学生 — 学习行为 | 1:N | 与N端合并 | `learning_behavior`表加`student_id`外键 |
| StudentTakeFamilyBackground | 学生 — 家庭背景 | 1:1 | 与家庭端合并 | `family_background`表加`student_id`外键+UNIQUE |
| StudentReceiveAlert | 学生 — 风险预警 | 1:N | 与N端合并 | `risk_alert`表加`student_id`外键 |
| StudentReceiveSuggestion | 学生 — 学习建议 | 1:N | 与N端合并 | `learning_suggestion`表加`student_id`外键 |
| StudentPlaceInClass | 学生 — 班级 | N:1 | 与N端合并 | `student`表加`StudentClassID`外键 |
| ClassHasSchedule | 班级 — 课程安排 | 1:N | 与N端合并 | `course_schedule`表加`ClassID`外键 |
| TeacherTeachesSchedule | 教师 — 课程安排 | 1:N | 与N端合并 | `course_schedule`表加`TeacherID`外键 |
| SubjectHasSchedule | 科目 — 课程安排 | 1:N | 与N端合并 | `course_schedule`表加`SubjectID`外键 |
| **StudentTakeSubject** | 学生 — 科目 | **M:N** | **独立成表** | **新建`student_subject`中间表** |

### 2.2 转换规则总结

**1:1联系转换**（StudentTakeFamilyBackground）：
- 与家庭背景端合并
- `student_id`作为外键加到`family_background`表
- 加`UNIQUE`约束确保1:1关系

**1:N联系转换**（8个）：
- 外键都加在N端
- 例如：StudentTakeExamScore，外键加在考试成绩表

**M:N联系转换**（StudentTakeSubject）：
- **必须**独立成一个新表
- 主键是两个外键的组合（联合主键）
- 这是本项目唯一的M:N联系

---

## 三、规范化检查结果

### 3.1 规范化检查总结表

| 表名 | 1NF | 2NF | 3NF | 备注 |
|------|-----|-----|-----|------|
| Teacher | ✅ | ✅ | ✅ | 独立实体，无外键 |
| Subject | ✅ | ✅ | ✅ | 独立实体，无外键 |
| Class | ✅ | ✅ | ✅ | 外键ClassTeacherID引用教师 |
| Student | ✅ | ✅ | ✅ | 外键StudentClassID引用班级 |
| ExamScore | ✅ | ✅ | ✅ | 双外键：student_id + subject_id |
| LearningBehavior | ✅ | ✅ | ✅ | 无传递依赖 |
| FamilyBackground | ✅ | ✅ | ✅ | UNIQUE约束保证1:1 |
| RiskAlert | ✅ | ✅ | ✅ | TEXT字段作为整体值 |
| LearningSuggestion | ✅ | ✅ | ✅ | 无传递依赖 |
| CourseSchedule | ✅ | ✅ | ✅ | 三外键：SubjectID + TeacherID + ClassID |
| StudentSubject | ✅ | ✅ | ✅ | M:N中间表，联合主键 |
| NL2SQLLog | ✅ | ✅ | ✅ | 独立日志表 |

**结论**：本项目所有12个表均满足第三范式（3NF），设计合理。

### 3.2 重点表检查说明

#### StudentSubject（M:N中间表）

**主键**：(student_id, subject_id) 联合主键

**1NF检查**：
- ✅ 每个字段都是单一值

**2NF检查**：
- ✅ `enroll_time`完全依赖于联合主键(student_id, subject_id)
- 不会出现只依赖student_id或只依赖subject_id的情况

**3NF检查**：
- ✅ 没有传递依赖
- `enroll_time`直接依赖于联合主键

**结论**：StudentSubject表满足3NF

#### CourseSchedule（课程安排表）

**主键**：ScheduleID

**1NF检查**：
- ✅ 每个字段都是单一值

**2NF检查**：
- ✅ 主键是单一属性

**3NF检查**：
- ✅ 没有传递依赖
- `SubjectID`、`TeacherID`、`ClassID`都是外键，直接依赖于ScheduleID

**结论**：CourseSchedule表满足3NF

---

## 四、建表顺序说明

### 4.1 建表顺序（重要）

因为外键约束的存在，必须先创建被引用的表，再创建引用它们的表：

| 顺序 | 表名 | 说明 |
|------|------|------|
| 1 | teacher | 独立表，无外键 |
| 2 | subject | 独立表，无外键 |
| 3 | class | 引用teacher |
| 4 | student | 引用class |
| 5 | exam_score | 引用student和subject |
| 6 | learning_behavior | 引用student |
| 7 | family_background | 引用student |
| 8 | risk_alert | 引用student |
| 9 | learning_suggestion | 引用student |
| 10 | course_schedule | 引用subject、teacher、class |
| 11 | student_subject | 引用student和subject（M:N中间表） |
| 12 | nl2sql_log | 独立日志表，无外键 |

### 4.2 如果顺序错了会怎样？

MySQL会报错：`Cannot add foreign key constraint`

**原因**：外键引用的表还不存在，无法创建外键约束。

---

> 本文档由组长邹梓辰整理，经小组讨论后确认。  
> E-R图来源：`合并ER图.drawio`  
> 本项目最终E-R图包含10个实体、10个联系（1个1:1、8个1:N、1个M:N），转换为12个关系模式。
